# README assets

Drop the screenshots referenced by the main README here:

- `signin.png` — the example app's "Sign in" step (the screen with the
  "Sign in with Google" button). No redaction needed.
- `account-picker.png` — the native Google "Choose an account" sheet that
  slides up after tapping the button. **Redact the email/account row** before
  committing (it's a public repo) — blur it or use a throwaway account.

Keep them roughly portrait (phone/tablet aspect). The README renders them at
280px wide side by side.
